export { getVirtualFileImportPageConfigs };
import { getVikeConfig } from './getVikeConfig.js';
import { serializePageConfigs } from './serializePageConfigs.js';
async function getVirtualFileImportPageConfigs(userRootDir, isForClientSide, isDev, id, configVps, isClientRouting) {
    const { pageConfigs: pageConfigsData, pageConfigGlobal } = await getVikeConfig(userRootDir, isDev, configVps.extensions, true);
    return serializePageConfigs(pageConfigsData, pageConfigGlobal, isForClientSide, isDev, id, isClientRouting);
}
